import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Meridian — Routing Intelligence for Stellar",
  description:
    "AI-powered route optimization engine for the Stellar network. Find the highest-quality paths for value transfer.",
  keywords: ["Stellar", "routing", "DeFi", "blockchain", "optimization"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} antialiased`}>{children}</body>
    </html>
  );
}
